{
  "name": "scer-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=scer\" \"npm run dev --workspace=audio-engine\" \"npm run dev --workspace=geopulse\" \"npm run dev --workspace=notification-engine\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present"
  },
  "devDependencies": {
    "@prisma/client": "^7.9.1",
    "@types/leaflet": "^1.9.22",
    "concurrently": "^10.0.5",
    "prisma": "^7.9.1",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0"
  }
}
